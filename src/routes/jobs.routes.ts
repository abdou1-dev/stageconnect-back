// Routes /api/jobs — TODO Phase 1+ : brancher les controllers
import { Router } from 'express'

const router = Router()

// Exemple : router.post('/', requireAuth, requireRole('COMPANY'), createjobs)

export default router
